diff -ur sane-backends-1.0.24.orig/configure.in sane-backends-1.0.24/configure.in
--- sane-backends-1.0.24.orig/configure.in	2013-10-01 03:00:39.000000000 +0200
+++ sane-backends-1.0.24/configure.in	2013-11-03 16:55:25.000000000 +0100
@@ -170,7 +170,7 @@
 		vers=`echo $snmp_version | awk 'BEGIN { FS = "."; } { printf "%d", ($1 * 1000 + $2);}'`
 		if test -n "$vers" && test "$vers" -ge 5006; then
 			SNMP_LIBS=`$SNMP_CONFIG_PATH --libs`
-			SNMP_CFLAGS=`$SNMP_CONFIG_PATH --cflags`
+			SNMP_CFLAGS= # This is broken `$SNMP_CONFIG_PATH --cflags`
 			AC_MSG_RESULT(yes)
 			with_snmp="yes"
 		else
diff -ur sane-backends-1.0.24.orig/configure sane-backends-1.0.24/configure
--- sane-backends-1.0.24.orig/configure	2013-10-01 03:01:13.000000000 +0200
+++ sane-backends-1.0.24/configure	2013-11-03 16:55:24.000000000 +0100
@@ -7969,7 +7969,7 @@
 		vers=`echo $snmp_version | awk 'BEGIN { FS = "."; } { printf "%d", ($1 * 1000 + $2);}'`
 		if test -n "$vers" && test "$vers" -ge 5006; then
 			SNMP_LIBS=`$SNMP_CONFIG_PATH --libs`
-			SNMP_CFLAGS=`$SNMP_CONFIG_PATH --cflags`
+			SNMP_CFLAGS= # This is broken `$SNMP_CONFIG_PATH --cflags`
 			{ $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
 $as_echo "yes" >&6; }
 			with_snmp="yes"
